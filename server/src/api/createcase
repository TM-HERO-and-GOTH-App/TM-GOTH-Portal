const db = require('../db')

async function asyncFunction() {
    try {
        // Insert values
        const H_ID = "Johnny";
        const CASE_NUM = "Bravo";
        const CLOSED_DATE = "jbravo@mariadb.com";
        const OWNER_ID = "";
        const OWNER_ID_SUPPORT = "";
        const C_TOKEN = "";

        //Auto Generated
        // const C_ID = "";
        // const CREATED_DATE = "";

        // Insert query with parameter placeholders
        const insertQuery = "INSERT INTO heroserver.tbl_case(first_name, last_name, email) VALUES (?, ?, ?)";

        // Insert new customer record using pool.query function
        const result = await db.pool.query(insertQuery, [first_name, last_name, email]);

        // Print results
        console.log(`Contact (id=${result.insertId}) inserted.`);
    } catch (err) {
        // Print errors
        console.log(err);
    }
}

asyncFunction();
